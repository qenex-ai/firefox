[video-loading-lazy-autoplay-when-visible.html]
  expected: ERROR
  [Video with loading=lazy and autoplay does not play while not visible, then plays once scrolled into view]
    expected:
      if not debug and (os == "android"): TIMEOUT
      if debug: TIMEOUT
      FAIL
